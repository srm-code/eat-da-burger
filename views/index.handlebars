<div class="container">
  <div class="jumbotron">
    <h1 class="text-center "><img class="logo" src="/assets/images/burger.png" alt="hamburger">&nbsp;Welcome to
      Eat-Da-Burger!
    </h1>
    <p>
      You can place orders for any kind of burger you want. See what happens to your burger when you click the "Devour
      It!" button.
    </p>
    <hr>
    <div class="text-center">
      <h2>Order A Burger Here</h2>
      <form class="create-form">
        <span class="form-group">
          <label for="ca">Burger Name:</label>
          <input type="text" id="ca" name="name">
        </span>
        &nbsp;
        <button class="btn btn-success btn-sm" type="submit">Add Burger</button>
      </form>
    </div>
    <hr>
    <div class="row">
      <div class="col-md-6">
        <h2 class="text-left">Burgers</h2>
        <ul>
          {{#each burgers}}
          {{#unless eaten}}
          {{> burgers/burger-block sleep=true}}
          {{/unless}}
          {{/each}}
        </ul>
      </div>
      <div class="col-md-6">
        <h2 class="text-left">Devoured!</h2>
        <ul>
          {{#each burgers}}
          {{#if eaten}}
          {{> burgers/burger-block sleep=false}}
          {{/if}}
          {{/each}}
        </ul>
      </div>
    </div> <!-- row -->
  </div> <!-- jumbotron -->
</div> <!-- container -->